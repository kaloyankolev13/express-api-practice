<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Comments</title>
  </head>
  <body>
    <a href="/comments/new">new</a>
    <h1>These are the comments</h1>
    <ul>
      <% for(let user of comments) { %>
      <li><b> <%=user.username %></b> : <%=user.comment %></li>
      <a href="/comments/<%= user.id %> ">Details</a>
      <a href="/comments/<%= user.id %>/edit">Edit</a>
      <% } %>
    </ul>
  </body>
</html>
